$('#{{ datatable.name }}').on('stateLoaded.dt', function (e, settings, data) {
    $('#{{ datatable.name }}-column-thead-{{ column.index }}, #{{ datatable.name }}-column-tfoot-{{ column.index }}')
        .val(data.columns[{{ column.index }}].search.search) ;
});
