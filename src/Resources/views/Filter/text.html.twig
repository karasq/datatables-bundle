<input id="{{ datatable.name }}-column-{{ location }}-{{ column.index }}"
       class="{{ column.filter.class }}"
       data-search-column-index="{{ column.index }}"
        {% if column.filter.placeholder != null %}
            placeholder="{{ column.filter.placeholder|trans }}"
        {% endif %}
       value="" />
